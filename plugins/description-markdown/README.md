# Description Markdown

A Quartz v5 transformer plugin that renders frontmatter descriptions through the configured
Markdown pipeline.

It supports the same text, Markdown, and HTML transforms used by page content while avoiding
recursive processing of generated descriptions.
