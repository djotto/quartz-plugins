# Frontmatter

A Quartz v5 transformer plugin that parses and strips YAML frontmatter while preserving the
metadata behavior used by otton.org.

It normalizes tags, aliases, permalinks, CSS classes, dates, and social images.
